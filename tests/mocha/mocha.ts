import 'mocha';
import { strictEqual } from 'assert';

describe('Mocha',
    function ()
    {
        it('is working.',
            function ()
            {
                strictEqual(true, true);
            }
        );
    }
);
